# =============================================================================
# .env.example — Environment variables for the HLS Streaming Pipeline
#
# Copy this file to .env and adjust values as needed before running:
#   cp .env.example .env
#   docker-compose up --build
# =============================================================================

# Path to the source video file (inside the processor container)
# Default: /app/video/source.mp4
SOURCE_VIDEO=/app/video/source.mp4

# Directory where HLS output will be written (inside the processor container)
# Default: /app/media/output
OUTPUT_DIR=/app/media/output

# Duration of each HLS segment in seconds (4–10 recommended)
# Shorter = lower latency but more overhead; longer = higher latency but fewer requests
# Default: 6
SEGMENT_DURATION=6

# Host port to expose the Nginx web server on
# Access the stream at http://localhost:<HOST_PORT>/media/output/master.m3u8
# Default: 8080
HOST_PORT=8080
